<nav class="navbar navbar-expand-lg navbar-dark bg-dark header">
  <a class="navbar-brand title" href="/">Sharing A Cup</a>
  <button class="navbar-toggler header-icon" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if login? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             <%= current_user.name %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "マイページ", user_path(current_user),class: "dropdown-item" %>
            <%= link_to "ユーザー情報の変更" ,edit_user_path(current_user) ,class: "dropdown-item" %>
            <%= link_to "新規投稿の作成", new_post_path , class: "dropdown-item" %>
            <%= link_to "ログアウト",logout_path ,class: "dropdown-item", method: :post %>
          </div>
          <li><%= link_to "投稿一覧", posts_path, class: "nav-link" %></li>
      </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "ログイン", login_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
           <%= link_to "新規登録", signup_path ,class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
